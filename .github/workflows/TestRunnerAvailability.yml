name: Test Runner Availability

on: workflow_dispatch

jobs:
  check-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Runner works
        run: echo "✅ This confirms runners are working"
